# --- Core NLP & Medical Entity Recognition ---
spacy>=3.5.0
scispacy>=0.5.1
# Note: You will also need a model like: 
# https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.1/en_core_sci_sm-0.5.1.tar.gz

# --- String Cleaning & OCR De-noising ---
re2>=2023.3.1          # Faster, safer regex handling
rapid-fuzz>=3.0.0      # For LASA (Look-Alike Sound-Alike) matching
thefuzz[speedup]>=0.19.0

# --- Data Validation & Structuring ---
pydantic>=2.0.0        # Essential for enforcing "mg" vs "mcg" schemas
pandas>=2.0.0          # For handling sliding scale tables or logs

# --- Utilities ---
python-dotenv>=1.0.0   # For environment variables
loguru>=0.7.0          # For clinical-grade error logging